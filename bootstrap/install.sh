#!/usr/bin/env bash
# weyland — fresh-Pi bootstrap installer
#
# Status: placeholder, not yet written.
#
# When complete, this script is the one-liner a fresh Pi runs:
#   curl -fsSL https://raw.githubusercontent.com/j-burton/weyland/main/bootstrap/install.sh | bash
#
# It will: install CC, install the weyland MCP connector, set up a
# Cloudflare tunnel, create a per-Pi repo, clone it, start a tmux
# session for CC, and print next-steps for Julian.
set -euo pipefail
echo "weyland bootstrap is not yet implemented."
exit 1
